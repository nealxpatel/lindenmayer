# 14.1 docs/kinds + test_kinds remainder

Scope per parent's continue directive (radio 4B99D37D): docs/kinds/ (9 files)
+ tests/test_kinds*.py only. No changes to src/lindenmayer/core/kinds/ models
unless a test exposes a real bug (none expected — models already smoke-tested
by hand per memory/status.md).

No delegation: max_children=0, this is a solo mechanical-writing pass.

## Steps

1. Scratch script (`tmp/gen_examples.py`) that imports the real models/registry
   and calls `to_event(pubkey=FIXED_PUBKEY, created_at=FIXED_TS)` for one
   instance per kind, using fixed dummy 64-hex values for all id-shaped
   fields (parent_pubkey, prev_event_id, approver_pubkey, request_id,
   inherits, git_ref n/a, version_event_id, last_lifecycle_event) so the
   examples are canonical-valid (real computed `id`) and deterministic.
   Print each as pretty JSON — these become the docs' worked examples
   verbatim, and the same fixed inputs get reused in tests so the doc
   examples and test fixtures are the same event.
2. Write the 8 kind docs (42010, 38110, 42020, 42030, 42040, 42041, 42050,
   38150) per §4 convention: title, kind number, range rationale (history
   vs pointer per §2.0), status: draft, full tag table (from event-kinds.md
   §2.x, cross-checked against the actual model), content JSON schema,
   worked example (from step 1), NIP-AM/AO correspondence note on
   42010/42020/42040/42041 (from memory's digested NIP-AM/AO vocab: costUsd/
   inputTokens/outputTokens/totalTokens/model/stopReason/sessionId/turnSeq/
   harness/timestamp for AM kind 44200; turn_started/session_resolved/
   cancel_turn frames for AO kind 24200 — note AM has NO duration field,
   correcting event-kinds.md §3's guess).
3. Write nip-oa-attestation.md: restatement of the cached NIP-OA.md (already
   read in full this iteration) — auth tag (4 elements), signing preimage
   `nostr:agent-auth:<agent-pubkey>:<conditions>`, SHA256+BIP-340, conditions
   grammar (kind=/created_at</created_at>, &-joined, canonical decimals, no
   whitespace, no self-attestation), the test vectors verbatim, pinned to
   buzz commit 06e3d82. Explicitly correct event-kinds.md §1.7's "presumed
   addressable" guess: NIP-OA is a tag, not a kind, so range semantics don't
   apply to it at all.
4. tests/test_kinds.py (split into test_kinds_models.py + test_kinds_docs.py
   if cleaner): per kind — valid round-trip (model->Event->model), wrong
   kind number rejected, missing required tag rejected, malformed content
   rejected, addressable kinds carry `d`. Plus a doc-example harness: extract
   each doc's ```json worked-example block, parse through `parse_event` via
   `Event.from_dict`, assert it matches the model instance from step 1 (skip
   nip-oa doc — validate its JSON example structurally instead: 4-element
   auth tag, hex lengths, and recompute the SHA256 preimage/sig check against
   the test-vector fields since Event.verify() needs real secp256k1 signing
   this repo's keys module can do — reuse `lindenmayer.core.keys` if it
   exposes schnorr verify, else assert structural shape only).
5. Run `bash .fractal/main.core.kinds/scripts/test.sh`, fix any failures.
6. Memory: prune status.md to terminal state (what stands, no remaining/NEXT
   lines). Wiki: nothing new durable to promote beyond what's already there
   (node_operations.md) — post one outbox line if so.
7. `fractal node finish` once docs+tests+test.sh all green.

Budget: ~$1.4 remaining after research reads this iteration. This is
mechanical writing across ~9 short files + one test file — comfortably fits.

