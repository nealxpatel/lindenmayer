# 10.1 Kind registry, docs, tests


Fresh build, no interrupted work. Solo leaf (child caps all 0 — no delegation
possible). Goal: land as much of the full deliverable set this iteration as
budget allows, in dependency order.

## Order of work (EXECUTE)

1. Read inputs: `docs/research/relay-integration/event-kinds.md` (§2 shapes,
   §2.0 ranges, §3 correspondences, §4 doc convention), skim DESIGN.md §4/§6,
   `src/lindenmayer/core/event.py` (frozen Event API), cached
   `tmp/nips/NIP-OA.md` + `NIP-AM.md` (+ AO for verdict correspondence).
2. `src/lindenmayer/core/kinds/`:
   - `constants.py` — single-source kind numbers (42010/42020/42030/42040/
     42041/42050/38110/38150), one edit point for renumbering.
   - `base.py` — shared pydantic-v2 base: kind check, required-tag extraction,
     content JSON (de)serialization, `to_event()`/`from_event()` plumbing,
     clear ValueError/ValidationError paths.
   - One module per kind (or grouped sensibly): eight models exactly per §2.
     No per-step model, no per-worker identity anywhere (privacy constraint);
     cost fields named `cost_shadow_usd`, NIP-AM-aligned names where §3 says.
   - `registry.py` — KIND_REGISTRY: number -> class; `parse_event(Event)`
     dispatcher. `__init__.py` exports (package-local only; core/__init__
     wiring is parent's job).
3. `docs/kinds/` — nine files per §4 convention (title, kind, range rationale,
   status draft, tag table, content JSON schema, one worked example event,
   NIP-AM/AO correspondence notes for 42010/42020/42040/42041).
   `nip-oa-attestation.md` = restatement of cached NIP-OA (auth TAG, not a
   kind): tag schema, signing preimage, conditions grammar, validation rules,
   pinned to buzz 06e3d82; explicitly correct event-kinds.md §1.7's
   "presumed addressable" guess.
4. `tests/` — `test_kinds.py` (+ split files if large): per kind accept
   (round-trip) + reject (wrong kind, missing tag, malformed content),
   `d`-tag presence for 38110/38150, and a doc-example harness that extracts
   each worked example JSON from docs/kinds/*.md and parses it through the
   registry (keeps docs and code in lockstep).
5. Run `bash .fractal/main.core.kinds/scripts/test.sh`; fix until green.

## Budget reality

~$5.4 node budget remains, 8 iterations max. Docs are the token-heavy part
(nine NIP-style files). If one iteration can't fit all of it: priority order
is models+registry+tests skeleton first (code green), then docs files, then
doc-example harness. Commit whatever is complete each iteration; memory note
records what stands vs what's missing. Do NOT finish until all nine docs +
tests green.

## Wind-down each iteration

Memory page `status.md`: what stands, decisions (field-name choices,
correspondence mapping), no iteration labels. Outbox: real progress line.
