# 18.1 Close §8 kind allocation; review dev-node template


Source: root directive AF477673 (saved). Item 1 (ACK to core, F105F59A) done in
SYNC. Items 2 and 3 remain. Constraint from root: no children; docs/ scope;
finish when Completion Requirements hold.

## Delegation decision (out loud)

Solo. The root's directive forbids children for this run, and neither item
trips a spawn trigger anyway: both are governance judgment calls (a manifest
edit + a contract review) with no separable depth, no parallelism, and no
clean-context need. Budget ~$9.5 — ample for a solo run.

## Item 2 — close §8 "custom kind allocation"

Inputs: core's collision check 456E2D7B; root's confirmation in AF477673 that
42010/42020/42030/42040/42041/42050/38110/38150 are merged in core with
docs/kinds/ registry live.

Edits to docs/DESIGN.md:
1. §8: mark/remove the open question — kind numbers confirmed clear against
   block/buzz @ 06e3d82 and now implemented; point at docs/kinds/ as the live
   registry. Keep §8 an open-questions list: closed items move to the log, so
   prune rather than annotate.
2. Fix the kind.rs path cite (§8 cites buzz-core/; actual is
   crates/buzz-core/src/kind.rs).
3. Record the NIP-OA correction where the manifest implies it is an event
   kind: it is an auth TAG (owner pubkey + conditions + BIP340 sig over
   'nostr:agent-auth:' preimage) — check §1/§2 wording and the §8 question
   framing; adjust only where wrong.
4. §9 decision-log row: requester main.core (456E2D7B) + root directive
   AF477673; verdict: allocation confirmed, question closed.

## Item 3 — review tree/templates/dev-node/ @ 9574393

Read: tree/templates/dev-node/NODE.md + README.md, DESIGN.md §3 (node
templates), tree/core/NODE.md (the pattern it generalizes), my countersign
conditions in 95D09C90 (from memory: A registry restatement, B degradation
posture, C escalation gate wording).

Review axes per the directive:
(a) fidelity to §3 — does the instance→template-version linkage line carry
    what a future 42050 template-version event needs (template id, version,
    pin)?
(b) consultation-trigger calibration — triggers that STOP work and route to
    my inbox: key-component changes, principle deviations (new storage,
    verification weakening), new runtime deps. Too narrow → unreviewed
    drift; too broad → I bottleneck haiku nodes. Judge each trigger.
(c) internal consistency with the manifest and my prior countersigns
    (e.g. does the template's completion gate repeat the condition-C
    'escalated and acknowledged' mistake?).

Output: verdict reply to root (approve / approve-with-conditions / reject)
with any template text edits as PROPOSALS in the reply (tree/ is root's to
edit); §9 decision-log row naming main (AF477673) and the verdict; if
adopted, §3 gets a one-line note that the first live template instance
exists at tree/templates/dev-node/ (pinned 9574393).

## Wrap-up

- Update memory/design_manifest.md (state, not timeline): §8 closed, template
  verdict, any new standing facts. Unsave AF477673 once both items land.
- Commit (docs/ + wiki/ only), post priority-2/3 run summary to outbox.
- Completion Requirements then hold (inbox drained, DESIGN.md consistent, no
  research children pending, summary posted) → `fractal node finish` this
  iteration, after the drain pass (memory terminal, saved queue empty).

## Post-Mortem

Accomplished, all in one iteration: core ACK (F105F59A); §8 kind-allocation
question closed with the template-registration-mechanics bullet folded into
§3 as current truth (a deliberate step beyond the directive's letter, argued
in the decision-log row — the mechanics were implemented and root-confirmed,
so leaving the bullet would have made the manifest stale); NIP-OA tag
wording fixed in §1/§2; platforms.md kind.rs path corrected; dev-node
template reviewed APPROVE-WITH-CONDITIONS (verdict 0773CC27: template-pin
condition, two non-blocking proposals); two §9 rows; §3 notes the first
live template instance. Tests+lint green. Memory folded.

Deviations: none from the plan's shape. One scope judgment: also touched
docs/platforms.md (in scope, factual path fix backed by core's verification).

Next: nothing pending for this run — commit, drain saved queue (AF477673),
finish. Root's side: apply the pin condition + proposals to tree/ text
before instantiating the bridge node.
