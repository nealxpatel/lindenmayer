# 2.1 Adopt DESIGN.md and close the handrolled era


Source: root directive A2C8C8C7 (saved). Adoption run, scoped to docs/.
Constraints: no research children, no wiki restructuring.

## Delegation decision

Solo. The root explicitly barred research children this run, and the task is
a single-document audit + edit — no separable depth, no parallelism, no
clean-context need. No spawn triggers fire.

## Tasks

1. **Consistency audit of DESIGN.md** (vs. itself and
   tree/platform-architect/NODE.md):
   - Cross-references checked: §4→§1, §4→§6.1, §5→§8, §3→§8 all correct.
   - Found: decision-log entry 2 cites "(§7)" for the compaction open
     question; it lives in §8. Historical entry → flag in outbox (task 2),
     do not edit.
   - Body fix needed: **Status** line still says "bootstrap draft" with
     changes landing "through the architect" prospectively — after
     commissioning (task 3) update Status to reflect active ownership.
   - NODE.md contract vs. DESIGN.md: no conflicts found (continuity open
     question present in §8; governance mode, scope, principles all match).
2. **Decision-log audit (§9):** all 6 entries handrolled by root's operator.
   Coherence check done; flag entry 2's §7→§8 citation error in outbox.
   Entry 3's "pending root review" on §3 remains accurate — leave.
3. **Commissioning entry:** add decision-log row — this node owns DESIGN.md
   and associated docs; handrolled operator channel closed; future design
   changes route through this node's inbox (priority 6) for review; merges
   to main remain the root's approval gate. Requested by: root (A2C8C8C7).
   Update Status line accordingly.
4. **Radio-topology note:** record that radio's one-hop subscription
   topology structurally enforces aggregates-up privacy. Root offered §5 or
   §8; judgment call at execute time — likely a short note under §6
   principle 1 fits best doctrinally, but staying within the offered
   sections (§5, where the evergreen node steers subgraphs via radio) is
   the lower-friction reading; explain placement in the outbox reply.
5. **Commit** (scoped, docs/ only), **reply to A2C8C8C7** with completion +
   flags, post progress summary (p2–3), write memory orientation page,
   unsave A2C8C8C7, then `fractal node finish` same iteration.

## Feasibility

~$1.6 spent of $12; single-file edits + radio traffic fits trivially in
remaining budget. Iteration 1 of 3 — aim to finish this iteration.

## Post-Mortem

Accomplishments: all four directive tasks (A2C8C8C7) done in one iteration —
Status line updated to active-manifest ownership; §5 radio-topology note added
(refs §6.1); commissioning + radio-topology decision-log rows added citing the
directive; §9 audit complete with entry 2's §7→§8 citation typo flagged to
root in reply E37B3CF0, not edited. Memory page design-manifest created.
Test/lint pass; diff is docs/DESIGN.md only.

Deviations: root offered §5 or §8 for the topology note; chose §5 (current
truth about a component, not an open question) and put the doctrinally-natural
§6.1 tie-in as a cross-reference rather than editing §6. Reasoning recorded in
the decision log and the reply.

Notes for next run: watch for root's response to the entry-2 typo flag — if
root directs a fix, it is a one-line edit plus no new log row (typo fix, not a
design decision; cite root's directive in the commit message).
