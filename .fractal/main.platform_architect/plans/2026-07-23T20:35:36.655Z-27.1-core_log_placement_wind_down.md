# 27.1 Core-log placement decision: consistency sweep and run wind-down


## Context

The run's directive work is DONE (in SYNC/PREPARE): root escalation D35D86E8
(Buzz relay rejects unknown kinds + enforces ±15-min created_at drift, both
verified @ daeaf7c) got verdict 4038F1D1 — option (a) adopted, core log moves
to the Lindenmayer relay, Buzz demotes to human surface; DESIGN.md §1/§6.2/§8
revised, two decision-log rows, committed (2879bab), parent merged, outbox
posted (A8CAFC45).

## Remaining work (this iteration, solo)

No delegation trigger fires: remaining work is a one-bullet doc edit plus
wind-down chores — sub-iteration chores stay the manager's. No children run.

1. **Consistency sweep result:** docs/ grep shows kind docs clean; the only
   stale surface is `docs/research/relay-integration/README.md` line ~67 —
   the minimum-relay-contract bullet (NIP-01+29+42 "and nothing more") now
   understates what a core-log carrier needs. Add a short bracketed update
   note pointing to DESIGN.md §1's two carrier requirements (accepts custom
   kinds; accepts historical created_at) with the decision-log trace
   (D35D86E8). Research stays history; the note is a pointer, not a rewrite.
2. **Wind-down drain (completion requirements all hold after step 1):**
   - re-check inbox for a root reply; act if one arrived
   - memory: prune to terminal state (no forward-looking lines; fold the
     decision facts already written)
   - wiki: nothing new promotes — DESIGN.md is the canonical home of the
     two-relay topology and node_operations already holds the ops facts;
     post the one-line outbox note saying so, per the drain rule
   - post priority-2 progress summary for the root
   - drain saved queue (currently empty), then `fractal node finish`
     in this same iteration.

Budget check: ~$4.5 remaining; the above is comfortably one iteration. Finish
now rather than idling two more iterations against the escalation channel —
the role is standing; the root continues me when the next request lands.
