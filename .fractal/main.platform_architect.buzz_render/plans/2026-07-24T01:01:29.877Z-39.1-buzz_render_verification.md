# 39.1 Attack claim C: Buzz tree/graph rendering

## Role
Solo verification node (max_children=0 — no fractal delegation). Will use the
Agent tool (Explore/general-purpose subagents, NOT fractal children) freely for
parallel read-only investigation within this iteration — that's just tool use,
not org delegation, and is cheap relative to the $1.2788 remaining this step's
budget (~$0.78 left after orientation).

## Orientation findings so far
- `~/Code/buzz` HEAD = daeaf7c (matches DESIGN.md's cited relay-ingest.rs
  commit), package.json/Cargo.toml don't show "0.4.24" directly but this is
  the checked-out ref the architect worked from — treat as canonical, note
  version-string caveat in report if not resolved.
- Repo root has FOUR client dirs, not one: `desktop/`, `web/`, `mobile/`,
  `admin-web/`. The evidence-so-far (TIMELINE_KINDS, MessageRow switch,
  routeTree.gen.ts) is 100% desktop-scoped. Nobody has looked at web/mobile/
  admin-web. This is axis 5 and looks like the biggest enumeration gap.
- No inbox messages from parent beyond the NODE.md brief; claim C's exact text
  isn't in docs/ (still in-flight upstream) — working from the brief's
  verbatim citations only, per instructions (don't re-derive them).

## Investigation workstreams (parallel Explore/general-purpose agents)

1. **Non-desktop clients** (biggest gap). For each of web/, mobile/,
   admin-web/: does it have its own message-kind allowlist / render switch?
   Is it more permissive than desktop's TIMELINE_KINDS+MessageRow closed
   switch? Does any of them embed a webview/iframe or richer markdown pipeline
   (rehype-raw, mermaid, etc.)? Do they share the same rendering code as
   desktop (monorepo shared package) or diverge?

2. **Image escape hatch** (most likely single refutation). Confirm PNG inline
   rendering end to end: upload path, size/dimension caps, whether an agent
   (not just a human) can post an image via the SDK/bot identity, whether GIF/
   animated images render, whether canvas (40100) also renders images, and
   whether multiple images can be laid out (i.e., can a single message or a
   canvas doc carry a large detailed graph image legibly).

3. **Markdown/canvas rendering pipeline depth.** What remark/rehype plugins
   are actually loaded (grep package.json + the markdown renderer setup) for
   BOTH message body and canvas (40100)? Specifically check for **mermaid**
   support (`remark-mermaid`, `rehype-mermaid`, or similar) — if canvas or
   messages render mermaid fenced code blocks, that is a direct, textual,
   agent-writable tree/graph renderer and a strong refutation of C. Also check
   GFM table nesting depth as a poor-man's tree fallback.

4. **24200 render classes in full.** Read the AgentSessionThreadPanel render
   dispatch and enumerate EVERY class (message/thought/plan/tool/permission/
   error/status/shell/file-edit/file-read/image) with what each actually
   renders — especially `plan` and `tool_call`: do they support nesting,
   indentation, or structured trees (e.g. a plan with sub-steps, a diff view)?

5. **Kind gate rigidity.** Is TIMELINE_KINDS truly the only gate in desktop,
   or is there a settings/feature-flag/dev-mode path (grep for
   feature-flag/experimental/config toggles touching kind lists) in any of
   the 4 clients?

6. **Extension points that aren't "plugins."** Bots/webhooks/slash-commands/
   custom emoji/link-unfurl-embeds/buzz:// deep link handler/Tauri webview
   windows — grep across desktop AND the other 3 clients.

## Execution plan for EXECUTE step
- Launch parallel Explore agents (foreground, single message, multiple tool
  calls) for workstreams 1–6, each scoped to specific dirs/greps, each
  required to return path:line evidence only (no inference).
- Synthesize into verdict: REFUTED if any surface found (mermaid rendering,
  richer non-desktop client, or a viable image-graph path); CONFIRMED only if
  all 6 come back genuinely empty; PARTIAL if desktop-specific claim holds but
  a non-desktop surface undercuts the product-level thesis.
- Write full findings to memory (per-workstream file:line notes + dead ends).
- Post radio verdict to parent per Completion Requirements format (a)-(e).
- Promote any durable Buzz-surface facts to wiki (new page or extend
  existing `event_kind_conventions`/`bridge_implementation` if relevant) —
  scrub iteration labels.
- `fractal node finish`.

## Budget check
$0.78 remaining this step / ~$3.5 remaining total node budget, 3 iterations
available, 20m/step. Six focused Explore agents in parallel should fit well
inside one iteration. No scope-down needed given budget is not the binding
constraint here — time and thoroughness are what matter for a verification
node whose job is exactly "don't miss a surface."


## Post-Mortem

Accomplishments: workstream 1 (non-desktop clients) substantially completed
via direct Bash/grep/read (not the planned Explore agents — all 5 parallel
Explore-agent launches failed simultaneously on a "Fable 5 limit" API error,
unrelated to this task; direct tool use was the working fallback). Confirmed
the repo has 4 clients (desktop/web/admin-web/mobile), that web/ is a git
browser (not a chat client — a false-friend "RepoTreeSection.tsx" name
notwithstanding) and admin-web/ is a bare admin panel (neither is a lead for
agent-activity rendering), and that mobile/ is a genuinely separate Flutter
chat client with a mobile-only `pulse`/`agent_activity_card.dart` surface not
present on desktop — read in full, and it turns out to be a flat
Twitter-like grouped-notes feed, not a tree/graph (same limitation shape as
desktop's 24200 panel, on a different client). Also got a clean negative
result on markdown/diagram tooling: zero hits for mermaid/d3/dagre/
cytoscape/reactflow/graphviz/plantuml across all 4 clients' JS package.json
files (Dart's pubspec.yaml not yet checked).

Deviation: hit the $1.5/iteration cost cap partway through EXECUTE (spent
~$1.56 in one iteration) with workstreams 2 (image escape hatch — the
single most-likely refutation per the brief), 3 (mermaid, partially done),
4 (24200 render classes), 5 (kind gate rigidity), and 6 (non-plugin
extension points) still undone. Per Reserve Mode instructions this iteration
winds down without calling `node finish` — the Completion Requirements
(post a graded verdict + memory holds findings) are not yet honestly
satisfiable with the thoroughness this task demands (an UNVERIFIED-heavy
report on the single most important axis, image-as-escape-hatch, would be a
weak deliverable for a node whose entire purpose is not missing a surface).

Next-iteration notes: iteration 2 should (a) resume investigation directly
via Bash/Grep/Read (skip Explore-agent parallelism, or retry with an explicit
`model:` override if reattempting subagents), (b) prioritize workstream 2
(image escape hatch) first since it's flagged as the most likely refutation,
then 4, 5, 6, and closing out the mobile pulse/activity reads, (c) send the
final graded completion-format radio report and call `fractal node finish`
once all 6 workstreams have at least a CONFIRMED/PARTIAL/UNVERIFIED grade
recorded (UNVERIFIED is an acceptable terminal grade for a workstream if
budget runs out again — just don't leave one totally unattempted).
