# 39.2 Finish Verification

## Situation
Iteration 1 spent its budget on workstream 1 (non-desktop clients) only, via
direct Bash/Grep/Read (parallel Explore-agent launch failed on an unrelated
API quota error — not retrying that path, it wastes budget on retries).
Workstreams 2 (image escape hatch — flagged most-likely refutation), 3
(mermaid — desktop/web/admin-web done, mobile pubspec.yaml not checked), 4
(24200 render classes), 5 (kind gate rigidity), 6 (non-plugin extension
points), plus two unread mobile files (note_card.dart, pulse_models.dart,
activity/*.dart) are still open. Budget this step: $0.92. Node total
remaining: ~$1.32 of $4 cap, iteration 2 of 3 (last chance after this is
iteration 3 — must land a postable verdict THIS iteration, budget permitting,
rather than risk a third partial pass).

## Plan for this iteration (EXECUTE step)
Work solo via direct Bash grep/read (no subagents — avoid repeat of iteration
1's failed parallel launch and its cost). Prioritize by refutation value per
dollar:

1. **Workstream 2: image escape hatch** (highest priority — most likely
   single refutation of C). In `~/Code/buzz` desktop/: find the image
   upload/render path (search MessageRow.tsx / attachment components for
   `img`, `imeta`, blossom/nip94-style file metadata). Confirm: (a) PNG
   renders inline — read the actual JSX; (b) size/dimension caps if any; (c)
   whether posting requires a human-driven upload flow or an agent/bot
   identity via buzz-sdk can construct an imeta tag directly (check
   crates/buzz-sdk builders for an image/attachment builder); (d) whether
   canvas (40100) also renders inline images (grep canvas renderer for `img`/
   `image`); (e) animated GIF handling if quick to check.

2. **Workstream 4: 24200 render classes.** Read
   AgentSessionThreadPanel and its per-class renderers in full (find the
   component file, likely near MessageRow or a dedicated agent-observer
   dir). Enumerate what `plan`, `tool_call`, `image` classes each actually
   render — nesting/indentation/structured tree support or not.

3. **Workstream 5: kind gate rigidity.** Grep for
   feature-flag/experimental/settings/config paths that touch kind lists or
   TIMELINE_KINDS in desktop; check for a dev-mode toggle.

4. **Workstream 6: non-plugin extension points.** Grep desktop (and quickly
   web/mobile) for bots/webhooks/slash-commands/custom-emoji/link-unfurl/
   embed/buzz:// deep-link window-opening, Tauri `WebviewWindow`/`shell.open`
   capepbilities that could load arbitrary content.

5. **Close workstream 1 fully**: read mobile pulse_models.dart, note_card.dart,
   and check mobile/pubspec.yaml for mermaid/graph-viz Dart packages (closes
   workstream 3 too). Quick, cheap — do first if time allows since files are
   already located.

Budget discipline: if $0.92 runs low before all 5 items are done, stop
investigating and grade whatever remains UNVERIFIED rather than overspend —
a graded, posted, honest report this iteration beats another partial-budget
exit. Track spend after each item; reserve ~$0.15 for writing memory + radio
report + finish.

## Synthesis (end of this iteration or start of next EXECUTE if budget forces
a 3rd iteration)
- Verdict: REFUTED if any surface found (image-as-graph, richer non-desktop
  render path, mermaid, or an extension point enabling arbitrary content);
  CONFIRMED only if all axes come back genuinely empty; PARTIAL if desktop
  specifically holds but some other axis (mobile, image hack) undercuts the
  product-level thesis.
- Update memory findings.md with full file:line detail for every workstream,
  including dead ends and UNVERIFIED items with exact search paths (so the
  enumeration gap stays visible per ground rules).
- Post radio verdict to parent in the mandated (a)-(e) format.
- Promote any durable Buzz-surface facts to project wiki if genuinely
  reusable by other nodes (e.g. "Buzz has 4 clients, only desktop's
  TIMELINE_KINDS gate is characterized" is wiki-worthy structural knowledge);
  otherwise note in outbox why nothing promotes.
- `fractal node finish`.

## Fallback if budget still runs out this iteration
This is iteration 2 of 3 — one more chance exists, but Completion
Requirements say never gate on a future iteration budget may not grant.
Prefer: post the best-available graded verdict (even if 1-2 axes remain
UNVERIFIED with explicit "did not check X" honesty) and finish THIS
iteration over risking iteration 3 not materializing. Only defer to
iteration 3 if genuinely zero workstreams beyond iteration 1's are gradable
yet (unlikely given item 5 is nearly free).
