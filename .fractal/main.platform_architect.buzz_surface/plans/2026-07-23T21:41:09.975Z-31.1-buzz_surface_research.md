# 31.1 Research Buzz Human-Surface Mapping

## Goal
Ground the Buzz human-surface mapping for the Lindenmayer evergreen node by researching how Buzz primitives work and mapping each Lindenmayer core event kind to corresponding Buzz surfaces.

Deliverable: `docs/research/evergreen/buzz-surface.md` with findings on Buzz primitives, ingest constraints, identity handling, and a complete mapping table.

## Decomposition Check
- Single research task, no decomposition triggers
- Solo work; research is finite and bounded
- No child nodes spawned
- Budget: $4.0/iter max, currently $1.2 remaining (plenty for focused research)

## Work Breakdown

### 1. Research Buzz Primitives (from ~/Code/buzz @ daeaf7c)
Target findings:
- NIP-29 group/channel mechanics in Buzz (rooms, membership model, invites, moderation, roles)
- Inbox surfaces (DM model, how they're created/managed)
- Agent directory (how agents surface in the product, what kind(s) power it)
- Event kinds that underpin each surface

Key files to read:
- `crates/buzz-core/src/kind.rs` — full list of accepted kinds
- `crates/buzz-relay/src/handlers/` — NIP-29 handling, DM routing, directory logic
- `crates/buzz-db/` — schema hints on channel/member/agent storage
- Desktop app code if available (desktop/ or similar)

### 2. Verify & Catalog Ingest Constraints
Target findings:
- Re-verify MAX_TIMESTAMP_DRIFT_SECS (claim: 900 = ±15 min)
- Exhaustive list of all accepted kinds from required_scope_for_kind()
- Filter to kinds usable for text messages/threads
- Per-kind scope requirements (MessagesWrite, ChannelsWrite, etc.)
- Any per-kind membership or channel-membership requirements

Key file:
- `crates/buzz-relay/src/handlers/ingest.rs` @ daeaf7c

### 3. Research Identity (NIP-OA Auth Tags)
Target findings:
- How NIP-OA `auth` tags are parsed/verified on Buzz ingest
- How attestation is surfaced to the user (e.g., in client UI or event display)
- What identity a Lindenmayer node would present (pubkey + auth tag)
- Degradation: what happens if auth tag is missing or invalid
- Nostr NIP-OA spec (if Buzz references it) vs. custom Buzz handling

Key searches:
- `auth` tag handling in ingest
- `nip_oa` or attestation in codebase
- Desktop/desktop/ product display logic for author identity

### 4. Build Complete Mapping Table
For each Lindenmayer core kind (from docs/kinds/):
- 42010: Node Lifecycle
- 42020: Run Accounting (Telemetry Rollups)
- 42030: Subgraph Digest (Radio Aggregates)
- 42040/42041: Approval Request/Verdict
- 42050: Template Version
- 38150: Template Pointer

For each, determine:
- Which Buzz-accepted kind(s) could carry a cross-post?
- What channel shape? (subgraph channel, approvals inbox, DM, directory entry, global?)
- Tag structure to carry source timestamp + core event id (per DESIGN.md §1 dedup-by-reference)
- Notes on uncertainties or tradeoffs

## Research Execution Order

1. Read Buzz kind.rs to catalog what's available
2. Read ingest.rs to understand constraints (kinds, timestamp)
3. Read NIP-29 handler and DM/inbox logic
4. Read agent directory logic
5. Search for NIP-OA/auth tag handling
6. Cross-reference with Lindenmayer kinds docs
7. Synthesize findings into mapping table and narrative

## Risk & Mitigation

- **Risk**: Buzz codebase large; may not find all surfaces (agent directory, inboxes)
  - Mitigation: grep for relevant terms (agent, inbox, directory, profile, identity)
  
- **Risk**: NIP-OA may be external spec; Buzz may not implement fully
  - Mitigation: Check what Buzz actually does with auth tags; don't assume spec compliance
  
- **Risk**: Cross-post tag structure is design choice, not yet implemented
  - Mitigation: That's OK; research documents the options, not mandates them

## Success Criteria
- Primitives section: NIP-29 channels, inboxes, agent directory explained with cited evidence
- Ingest section: timestamp constraint re-verified, all message-capable kinds listed, scope/membership requirements noted
- Identity section: NIP-OA handling on Buzz ingest and display documented
- Mapping table: all 8 Lindenmayer kind families × Buzz kind/shape/tags populated with notes
- All claims cited as file:line or file@commit

